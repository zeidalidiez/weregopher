{
  "$defs": {
    "ArtifactTrustEvidenceDigest": {
      "$ref": "#/$defs/Sha256Digest",
      "description": "Artifact trust-evidence identity."
    },
    "ExecutableDigest": {
      "$ref": "#/$defs/Sha256Digest",
      "description": "Exact executable byte identity."
    },
    "ExecutionArtifactLocator": {
      "description": "Exact artifact locator interpreted only within the source declared by one target contract.",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Exact manifest-allowlisted package-relative path.",
          "properties": {
            "artifact_source": {
              "const": "package_snapshot",
              "type": "string"
            },
            "normalized_path": {
              "$ref": "#/$defs/ExecutionPackagePath",
              "description": "Canonical forward-slash package-relative path."
            }
          },
          "required": [
            "artifact_source",
            "normalized_path"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Exact blob in a retained managed-artifact manifest.",
          "properties": {
            "artifact_source": {
              "const": "managed_artifact",
              "type": "string"
            },
            "digest": {
              "$ref": "#/$defs/ExecutableDigest",
              "description": "Content identity of the executable blob."
            }
          },
          "required": [
            "artifact_source",
            "digest"
          ],
          "type": "object"
        }
      ]
    },
    "ExecutionArtifactSourceDigest": {
      "$ref": "#/$defs/Sha256Digest",
      "description": "Package-tree or managed-manifest identity containing an executable."
    },
    "ExecutionContractDigest": {
      "$ref": "#/$defs/Sha256Digest",
      "description": "Canonical static execution-target contract identity."
    },
    "ExecutionPackagePath": {
      "description": "Canonical package-relative Windows path. Rust validation also rejects DOS device aliases in every component.",
      "maxLength": 4096,
      "minLength": 1,
      "pattern": "^(?!/)(?!.*//)(?!.*(?:^|/)\\.{1,2}(?:/|$))(?!.*[<>:\"\\\\|?*\\u0000-\\u001f])(?!.*[. ](?:/|$)).+$",
      "type": "string",
      "x-weregopher-maxPathComponents": 256,
      "x-weregopher-maxUtf8Bytes": 4096,
      "x-weregopher-windowsDeviceAliasesRejected": true
    },
    "ExecutionResolutionDigests": {
      "additionalProperties": false,
      "description": "Role-named generated resolution-evidence identities.",
      "properties": {
        "artifact_source_digest": {
          "$ref": "#/$defs/ExecutionArtifactSourceDigest",
          "description": "Exact package-tree or managed-manifest identity."
        },
        "artifact_trust_evidence_digest": {
          "$ref": "#/$defs/ArtifactTrustEvidenceDigest",
          "description": "Exact signer, local-build trust, or equivalent artifact trust evidence identity."
        },
        "executable_digest": {
          "$ref": "#/$defs/ExecutableDigest",
          "description": "Exact executable byte identity."
        },
        "execution_contract_digest": {
          "$ref": "#/$defs/ExecutionContractDigest",
          "description": "Exact static target-contract identity."
        },
        "provenance_evidence_digest": {
          "$ref": "#/$defs/ProvenanceEvidenceDigest",
          "description": "Exact artifact provenance evidence identity."
        }
      },
      "required": [
        "execution_contract_digest",
        "artifact_source_digest",
        "executable_digest",
        "artifact_trust_evidence_digest",
        "provenance_evidence_digest"
      ],
      "type": "object"
    },
    "ExecutionResolutionFormatVersion": {
      "enum": [
        "2"
      ],
      "type": "string"
    },
    "ExecutionTargetId": {
      "description": "A durable static execution-target identifier.",
      "maxLength": 255,
      "minLength": 1,
      "pattern": "^(?!.*\\.\\.)[a-z0-9](?:[a-z0-9._-]{0,253}[a-z0-9])?$",
      "type": "string"
    },
    "ProvenanceEvidenceDigest": {
      "$ref": "#/$defs/Sha256Digest",
      "description": "Artifact provenance-evidence identity."
    },
    "Sha256Digest": {
      "description": "A SHA-256 digest serialized as lowercase `sha256:<hex>` text.",
      "maxLength": 71,
      "minLength": 71,
      "pattern": "^sha256:[0-9a-f]{64}$",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Generated exact resolution evidence for one static execution target.",
  "properties": {
    "artifact_locator": {
      "$ref": "#/$defs/ExecutionArtifactLocator"
    },
    "digests": {
      "$ref": "#/$defs/ExecutionResolutionDigests"
    },
    "format_version": {
      "$ref": "#/$defs/ExecutionResolutionFormatVersion"
    },
    "target_id": {
      "$ref": "#/$defs/ExecutionTargetId"
    }
  },
  "required": [
    "format_version",
    "target_id",
    "artifact_locator",
    "digests"
  ],
  "title": "ExecutionResolutionEvidence",
  "type": "object",
  "x-weregopher-managedLocatorDigestEqualsExecutableDigest": true,
  "x-weregopher-maxDocumentBytes": 65536
}
