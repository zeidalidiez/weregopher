# Architecture decision records

ADRs capture implementation choices that refine, but do not silently override, locked product decisions. Accepted ADRs are immutable; superseding decisions create a new ADR that links to the old record.

Statuses: `Proposed`, `Accepted`, `Superseded`, `Rejected`.
